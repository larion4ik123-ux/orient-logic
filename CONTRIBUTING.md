# Руководство по участию в разработке Orient Logic

Спасибо за интерес к улучшению сайта Orient Logic! Это руководство поможет вам внести свой вклад в проект.

## 📋 Оглавление

- [Начало работы](#начало-работы)
- [Процесс разработки](#процесс-разработки)
- [Стиль кода](#стиль-кода)
- [Коммиты](#коммиты)
- [Pull Requests](#pull-requests)

## 🚀 Начало работы

### Предварительные требования

- Node.js 18+
- npm или yarn
- Git
- Базовые знания React, Next.js, TypeScript

### Настройка проекта

1. **Форк репозитория**
   
   Нажмите кнопку "Fork" на GitHub

2. **Клонирование форка**
   ```bash
   git clone https://github.com/YOUR_USERNAME/orient-logic.git
   cd orient-logic
   ```

3. **Установка зависимостей**
   ```bash
   npm install
   ```

4. **Создание .env.local**
   ```bash
   cp .env.example .env.local
   ```

5. **Запуск dev-сервера**
   ```bash
   npm run dev
   ```

## 💻 Процесс разработки

### Создание ветки

Создайте новую ветку для вашей функции или исправления:

```bash
git checkout -b feature/your-feature-name
# или
git checkout -b fix/your-bug-fix
```

Именование веток:
- `feature/` — новая функциональность
- `fix/` — исправление бага
- `docs/` — изменения в документации
- `style/` — изменения стилей
- `refactor/` — рефакторинг кода

### Разработка

1. Внесите изменения в код
2. Тестируйте изменения локально
3. Убедитесь, что код соответствует стандартам проекта

### Тестирование

```bash
# Проверка типов TypeScript
npm run type-check

# Линтинг
npm run lint

# Сборка проекта
npm run build
```

## 📝 Стиль кода

### TypeScript

- Используйте строгую типизацию
- Избегайте `any`, используйте конкретные типы
- Используйте интерфейсы для объектов

```typescript
// ✅ Хорошо
interface User {
  name: string
  email: string
}

function greetUser(user: User): string {
  return `Hello, ${user.name}!`
}

// ❌ Плохо
function greetUser(user: any) {
  return `Hello, ${user.name}!`
}
```

### React Components

- Используйте функциональные компоненты
- Используйте TypeScript для пропсов
- Предпочитайте именованный экспорт

```typescript
// ✅ Хорошо
interface ButtonProps {
  onClick: () => void
  children: React.ReactNode
  variant?: 'primary' | 'secondary'
}

export function Button({ onClick, children, variant = 'primary' }: ButtonProps) {
  return (
    <button onClick={onClick} className={`btn-${variant}`}>
      {children}
    </button>
  )
}

// ❌ Плохо
export default function Button(props) {
  return <button onClick={props.onClick}>{props.children}</button>
}
```

### CSS/Tailwind

- Используйте Tailwind CSS классы
- Создавайте переиспользуемые классы в `globals.css`
- Следуйте mobile-first подходу

```tsx
// ✅ Хорошо
<div className="container-custom section-padding">
  <h2 className="section-title">Заголовок</h2>
</div>

// ❌ Плохо
<div style={{ padding: '20px', maxWidth: '1200px' }}>
  <h2 style={{ fontSize: '32px', fontWeight: 'bold' }}>Заголовок</h2>
</div>
```

## 💬 Коммиты

Используйте [Conventional Commits](https://www.conventionalcommits.org/):

```
<type>(<scope>): <subject>

<body>

<footer>
```

### Типы коммитов:

- `feat:` — новая функциональность
- `fix:` — исправление бага
- `docs:` — изменения в документации
- `style:` — форматирование, пропущенные точки с запятой и т.д.
- `refactor:` — рефакторинг кода
- `test:` — добавление тестов
- `chore:` — обновление зависимостей, конфигурации и т.д.

### Примеры:

```bash
# Новая функция
git commit -m "feat(contacts): add WhatsApp contact button"

# Исправление бага
git commit -m "fix(form): resolve validation error on submit"

# Документация
git commit -m "docs(readme): update deployment instructions"

# Рефакторинг
git commit -m "refactor(header): simplify navigation component"
```

## 🔀 Pull Requests

### Перед созданием PR

1. Убедитесь, что ваш код работает
2. Запустите линтинг: `npm run lint`
3. Проверьте сборку: `npm run build`
4. Обновите документацию, если необходимо

### Создание PR

1. Пуш вашей ветки:
   ```bash
   git push origin feature/your-feature-name
   ```

2. Откройте PR на GitHub

3. Заполните описание PR:
   ```markdown
   ## Описание
   Краткое описание изменений
   
   ## Тип изменений
   - [ ] Новая функция
   - [ ] Исправление бага
   - [ ] Документация
   - [ ] Рефакторинг
   
   ## Чеклист
   - [ ] Код следует стилю проекта
   - [ ] Тесты пройдены
   - [ ] Документация обновлена
   ```

### Ревью кода

- Будьте открыты к обратной связи
- Отвечайте на комментарии конструктивно
- Вносите запрошенные изменения

## 🐛 Сообщение о багах

При обнаружении бага создайте Issue с:

1. **Заголовок** — краткое описание проблемы
2. **Описание** — детальное описание проблемы
3. **Шаги воспроизведения** — как воспроизвести баг
4. **Ожидаемое поведение** — что должно происходить
5. **Фактическое поведение** — что происходит на самом деле
6. **Скриншоты** — если применимо
7. **Окружение** — браузер, ОС, версия Node.js

## 💡 Предложения

Есть идея для улучшения? Создайте Issue с:

1. **Заголовок** — краткое описание предложения
2. **Описание** — детальное описание идеи
3. **Мотивация** — почему это улучшение необходимо
4. **Альтернативы** — рассматривали ли вы другие варианты

## 📞 Связь

Если у вас есть вопросы:

- Email: orientlogic@bk.ru
- Telegram: [@orientlogic](https://t.me/+79186798706)

## 📄 Лицензия

Внося свой вклад, вы соглашаетесь, что ваши изменения будут лицензированы под той же лицензией, что и основной проект.

---

**Спасибо за ваш вклад! 🙏**
